# connect the pod expose and make it accessible, created a service yaml
#
kind: Service
apiVersion: v1
metadata:
  name: frontend-service
spec:
  selector:
    tier: front-end
    app: App1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP


